# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
.vscode/

# Local documentation
docs_tmp/
# Reference materials are local-only (PDFs, summaries, notes)
docs/references/

# Internal milestone and planning docs (not for public repo)
docs/devel/milestone_*.md

# Build artifacts
results/

# Only ignore the binary outputs, not build configuration
cmd/gopca-desktop/build/bin/
cmd/gocsv/build/bin/

# Legacy build directory if it exists elsewhere
build/
cmd/gocsv/gocsv
cmd/gopca-desktop/gopca-desktop

# Node modules
node_modules/

# Python
__pycache__/
.venv/

# Temporary images
*.tmp.png

# Certificate files (never commit!)
.certs/
*.p12
*.pfx

# MSIX packaging artifacts
cmd/gopca-desktop/AppxManifest.xml
*.msix
*.appx

# Misc. temp files
_tmp/
testdata/wine/plotly/
testdata/validation/reference_results
